import { UserModel } from './user';
import { SessionModel } from './session';
import { AICompanionModel } from './ai_companion';

export const dBEntities = {
    Users: UserModel,
    Sessions: SessionModel,
    AICompanions: AICompanionModel
};

